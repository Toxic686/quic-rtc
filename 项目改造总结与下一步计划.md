# 项目改造总结与下一步计划

## 1. 改造总结

### 1.1 SDK化重构
完成了从“验证原型”到“SDK”的架构转变。
*   **配置接口化**：移除了硬编码的环境变量（如 `RTC_QUIC_CC_ALGO`），转而通过 `rtc::Configuration` 结构体暴露 C++ API。
*   **拥塞控制支持**：在 `rtc::Configuration` 中新增 `quicCongestionControl` 选项，支持 `Cubic`、`BBRv1` 和 `Adaptive` 算法。
*   **依赖管理自动化**：
    *   引入 `CMake FetchContent` 模块，实现了 `BoringSSL` 的自动下载与编译，彻底解决了用户需手动编译安装 BoringSSL 的痛点。
    *   移除了对 `libevent` 的依赖（通过禁用 lsquic 的 test/example 编译）。
    *   构建过程现在只需标准的 `cmake` 和 `make` 命令。

### 1.2 性能验证
通过跨洲（Server <-> Local）的分布式测试，验证了 QUIC DataChannel 的性能优势：
*   **高延迟/高丢包场景**：QUIC 的 BBR 算法表现出显著优势。
*   **大包传输**：在 1MB+ 数据包场景下，QUIC 吞吐量是 SCTP 的 10 倍以上（避免了 SCTP 的队头阻塞和缓冲区问题）。
*   **稳定性**：修复了 SCTP 在大包传输下的超时问题，并验证了 QUIC 连接的长期稳定性。

### 1.3 代码清理
*   移除了冗余的 shell 测试脚本（`distributed_test.sh` 等），保留了核心的 C++ 示例。
*   清理了代码中的调试日志和 AI 生成痕迹，使其更符合开源项目的代码规范。

### 1.4 功能增强
*   **QUIC Datagram 支持**：实现了基于 RFC 9221 的 QUIC Datagram 扩展。现在 DataChannel 可以根据 reliability 参数（`maxRetransmits`, `maxPacketLifeTime`, `unordered`）自动选择底层的传输模式：
    *   **可靠模式**：使用 QUIC Stream，保证有序可靠到达。
    *   **不可靠模式**：使用 QUIC Datagram，无队头阻塞，适用于实时音视频/游戏场景。
*   **项目更名**：项目正式更名为 **KratosRTC**，库名更新为 `libkratos-rtc`，并更新了所有相关的构建目标和文档。

## 2. 下一步计划

### 2.1 技术优化 (BBR & 传输)
*   **BBR 参数调优 (核心行动一)**：[已完成] 已通过 `rtc::Configuration` 暴露 lsquic 内部的 BBR 调优参数，无需修改源码即可调整：
    *   `bbrMinRttExpiry` (默认 10s): 最小 RTT 探测周期。实时场景下可尝试设为 0 或极大值以禁用 ProbeRTT 导致的 200ms 停顿。
    *   `bbrInitCwnd` (默认 32): 初始拥塞窗口。
    *   `bbrCwndGain` (默认 2.0): ProbeBW 阶段的增益。
    *   `bbrPacingGain` (默认 2.885): Startup 阶段的 Pacing 增益。
*   **显式 Pacing 控制**：目前默认启用 lsquic 的 Pacing。需进一步验证在 DTLS 隧道下的 Pacing 精确性，必要时在应用层实现补充 Pacing。
*   **0-RTT / Zero-copy (核心行动三)**：探索 QUIC 的高级特性以进一步降低首包延迟和 CPU 开销。考虑引入 eBPF 优化内核路径，或分析 TLS 1.3 密钥交换对 CPU 的影响。

### 2.2 协议与场景拓展 (核心行动二)
*   **WebTransport 支持**：利用 W3C 基于 QUIC 的新标准，拓展实现 WebTransport 服务端，打通 Web 前端生态，使前端 JS 能直接建立 QUIC 连接。
*   **HTTP/3 互联**：研究 QUIC DataChannel 与标准 HTTP/3 服务的互联互通性，打开更广阔的应用场景。

### 2.3 完善 SDK 封装
目前 `webrtc_client.cpp` 仍是一个较大的示例文件，下一步应将核心逻辑（DataChannel 的管理、性能统计、可靠性封装）提取为独立的 C++ 类库，供用户直接调用。

### 2.2 增强文档
*   补充 API Reference 文档，详细说明 `Configuration` 中各个 QUIC 参数的含义。
*   提供更多场景的示例代码（如文件传输、即时通讯）。

### 2.3 社区建设
*   提交代码到 GitLab/GitHub。
*   设置 CI/CD 流水线，确保在不同平台（Linux, macOS, Windows）下的编译兼容性。
*   撰写 Contributing Guide，鼓励社区参与。

### 2.4 技术深挖
*   **BBRv2/v3 探索**：目前 lsquic 主要支持 BBRv1，未来可关注 BBRv2/v3 的集成，以进一步优化公平性和丢包容忍度。
*   **WebTransport 支持**：探索基于 QUIC 的 WebTransport 协议支持，以实现与浏览器的原生 QUIC 通信。
